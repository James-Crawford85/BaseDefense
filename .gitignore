# Godot 4 cache/imported files
.godot/

# Exports
export_presets.cfg
*.pck
*.zip

# Loose images at the repo root are scratch/downloads — canonical copies live in assets/
/*.png
/*.png.import

# Exported builds are distributed as GitHub Release assets, not committed
/build/

